# v0.0.149 - 2026-04-24

`shutil`: `copy`, `copy2`, `copyfile`, `copyfileobj`, `copymode`, `copystat`,
`copytree`, `rmtree`, `move`, `disk_usage`, `which`, `make_archive`,
`unpack_archive`, `get_archive_formats`. `os.chmod` added.

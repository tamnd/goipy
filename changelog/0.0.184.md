# v0.0.184 - 2026-04-24

`curses`: `initscr`, `newwin`, `wrapper`. Window methods: `addstr`, `addch`,
`getch`, `refresh`, `clear`, `border`, `box`, `move`, `getyx`, `getmaxyx`,
`keypad`, `scrollok`, `resize`. `cbreak`, `noecho`, `start_color`, `init_pair`.

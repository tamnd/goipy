# v0.0.330 — 2026-04-28

syslog deep coverage -- 7 missing macOS facility constants and fixture 330

Added `LOG_AUTHPRIV`, `LOG_FTP`, `LOG_NETINFO`, `LOG_REMOTEAUTH`, `LOG_INSTALL`,
`LOG_RAS`, and `LOG_LAUNCHD` to the syslog stub, completing the full macOS
facility constant set (38 constants total).

Fixture 330 asserts all constants with exact CPython macOS values, verifies
all six functions are callable, checks `LOG_MASK`/`LOG_UPTO` formulas, confirms
`setlogmask` returns `int`, and calls `syslog`/`openlog`/`closelog` without
raising.

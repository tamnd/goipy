# v0.0.314 - 2026-04-28

`dis` module — fixture 314 for https://docs.python.org/3/library/dis.html. Full CPython 3.14 dis API surface.

**New `vm/stdlib_dis.go` — `buildDis()`:**

- **`HAVE_ARGUMENT = 43`**, **`EXTENDED_ARG = 69`** — opcode constants
- **`cmp_op`** — tuple `('<', '<=', '==', '!=', '>', '>=')` (6 entries)
- **`opmap`** — dict mapping opname→opcode (154 entries, CPython 3.14 exact)
- **`opname`** — 267-element list indexed by opcode number
- **`COMPILER_FLAG_NAMES`** — dict `{1: 'OPTIMIZED', ..., 134217728: 'METHOD'}` (12 entries)
- **Opcode category lists:** `hasarg` (101 entries), `hasconst`, `hasname`, `hasjump`, `hasjrel`, `hasjabs`, `hasfree`, `haslocal`, `hasexc`, `hascompare`
- **`Positions`** — namedtuple-like class; `_fields = ('lineno', 'end_lineno', 'col_offset', 'end_col_offset')`
- **`Instruction`** — namedtuple-like class; `_fields` with 12 entries; supports `_replace()`
- **`Bytecode(x)`** — class with `codeobj`, `first_line`, iterable stub, `dis()` method stub
- **`code_info(x)`** → `str` stub (returns `""`)
- **`show_code(x, file=None)`** → `None` stub
- **`dis(x, file=None, ...)`** → `None` stub
- **`disassemble(co, ...)`** → `None` stub
- **`distb(tb=None, ...)`** → `None` stub
- **`disco`** — alias for `disassemble`
- **`get_instructions(x, ...)`** → empty list stub
- **`findlinestarts(code)`** → empty list stub
- **`findlabels(code)`** → empty list stub
- **`stack_effect(opcode, oparg=None, ...)`** → `int` stub returning `0`

Registered `"dis"` in `vm/asyncio.go`.

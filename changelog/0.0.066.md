# v0.0.066 - 2026-04-21

`json`: encoder, decoder, `loads`, `dumps`, indent/separators/sort_keys.
`re`: `compile`, `search`, `match`, `findall`, `sub`, flags. `string`:
Template. `copy`: `copy`, `deepcopy`.
